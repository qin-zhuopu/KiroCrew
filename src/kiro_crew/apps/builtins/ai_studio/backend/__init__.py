"""AI Studio backend: the project store and its HTTP routes.

Re-exported through the package's ``__init__`` (the ``register_routes``
convention the gateway's startup scan checks), like crew_companion.
"""
