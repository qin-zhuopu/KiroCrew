# 产品原型智能演示设计方法论（AI Studio 适配版）

一句话总纲：**好的演示不是模拟用户点击页面，而是重放一个业务事件驱动的状态机——用可回放的状态快照，让用户看到系统"为什么"这样变化。**

（源头方法论沉淀自 Agent 工作负载页 / AI Coding 平台 / 文档版本 Diff / 图谱变更等 Demo；本文件是其面向 KiroCrew AI Studio 文档工作台的适配与落地规范。）

## 一、演示的目标是"业务闭环"，不是页面跳转

每一段演示必须回答四问：为什么发生变化？谁触发的？系统内部发生了什么？结果是什么？

AI Studio 的标准闭环（本应用的主业务链，演示必须演它）：

```
用户编辑 requirements.md
  → 防抖 2s 自动保存，产生一条草稿记录
  → diff 图标由灰变亮（有未提交修改）
  → 用户点 diff，看到 红删绿增
  → 用户点提交
  → 旧内容进版本快照(vN)、修改历史清空、diff 图标回灰
```

反例（禁止）："点图标 → 弹层出现 → 关闭"——这是 UI 动画，不是业务。

## 二、先设计状态机，再写演示脚本

对象与状态：

- **文档**：`干净` → `草稿(有未提交修改, 含 N 条自动保存记录)` → `已提交(vN)`
- **diff 图标**：`灰`（当前内容==最近提交）/ `亮`
- **修改历史图标**：`灰`（上次提交以来 0 条）/ `有 N 条`（提交后清零）
- **版本历史**：`v1..vN`，每版携带"vs 上一版"的 diff；v1 无前驱，整篇按新增

迁移规则（脚本的每个 action 必须落在规则内）：

```
干净 --编辑+防抖--> 草稿(+1条记录；内容与上条相同则不落新条)
草稿 --提交--> 已提交(vN+1；快照进 versions/，草稿记录清空)
草稿 --恢复某条自动保存--> 草稿(当前内容=该条)
已提交 --编辑--> 草稿
```

页面只是状态的展示；脚本先改状态机，再对页面。

## 三、演示步骤 = 业务事件，不是按钮动作

每个 Step 的统一结构：

```json
{
  "title": "编辑需求文档后，自动保存生效",
  "before": { "doc": "requirements.md", "diffIcon": "gray", "draftRecords": 0 },
  "action": { "event": "用户追加一行验收标准，停顿触发防抖" },
  "after":  { "diffIcon": "active", "draftRecords": 1 },
  "highlight": { "target": "[data-testid=doc-editor] .toolbar-history", "hint": "图标亮起：存在未提交的自动保存" }
}
```

即每一步都是 **事件 + 状态迁移 + UI 反馈** 三件套。

## 四、演示必须有"视觉引导层"

- 每步给 **高亮目标**（selector + 描边）与 **当前提示文案**（"当前：请查看 diff 图标的红绿行"）
- 引导层是**独立的 overlay 组件**（高亮框、提示条、上一步/下一步/自动播放的步进器），演示模式由 URL 参数（如 `?demo=<剧本名>`）注入——**业务组件内不许出现任何"为了演示"的代码**
- 弹层/菜单的展开也是引导层的事：脚本说"现在该看修改历史面板"，引导层负责把它打开并指过去

## 五、演示不改真实数据：状态快照，可回放

- 每个 Step 携带一份完整状态快照（fixtures JSON），对应后端 store 的一份数据（演示模式下把数据层指向快照）
- 下一步 = 加载 State(n+1)；上一步 = 加载 State(n-1)；**禁止反向计算/反向操作恢复**
- 演示全程写独立 home（KIROCREW_HOME 指 demo 目录），**绝不碰用户/演示环境的真实项目数据**

## 六、状态层与展示层分离（回放稳定性的根）

- **状态层**：现在有哪些文档/草稿/版本、内容是什么——快照驱动
- **展示层**：现在高亮什么、提示什么、该开哪个面板——剧本驱动
- 禁止 `step 执行 → 改数据 → setTimeout 开弹窗` 的耦合写法（回退残留、自动播放冲突、CDP 不稳三坑都源于它）
- 回退 = 状态层整体换快照 + 展示层按该步剧本重放，两者各自幂等

## 七、必须覆盖异常分支，不止 happy path

AI Studio 至少要演：

- 无修改时 diff 图标置灰不可点（为什么灰 → 状态机里"当前==最近提交"）
- 修改历史为空（刚提交完）——顺便讲"提交即清空"的规则
- 最早版本无上一版 → diff 整篇为新增
- 从一条自动保存记录**恢复**的分支（草稿态回到过去）

分支步骤与主线共用同一套 Step 结构，剧本里标 `branch: "alt-N"`。

## 八、Playwright 测的是剧本，不是页面

剧本测试必须验证四件事（"按钮存在/文字存在"级别的测试不算数）：

1. **状态迁移**：执行 step 后 before/after 声明的状态逐项为真（DOM data 属性或 store 断言）
2. **引导落位**：当前步的 highlight selector 命中且可见
3. **回放一致**：手动"下一步"×N 的最终状态 == 自动播放到第 N 步的状态
4. **回退恢复**：走到 step5 → 上一步 → 页面与快照 State4 完全一致（最容易出 bug 的一处）

测试跑在演示模式 + 独立 home 上；CDP 调用一律带 timeout 与总闸。

**第 0 项·schema 预检（最先跑，fail-fast）**：加载全部快照与剧本，运行时校验快照结构符合真实 API 响应类型（与第八节"类型同构"呼应：类型只有一个来源），并校验剧本自洽（before/after 引用的字段在快照存在、selector 非空）。预检不过，后面四组断言一律不执行——数据错着播演出来的"成功"全是假的。

## 九、标准目录结构

```
website/src/apps/ai-studio/demo/
  scenario.md              # 业务故事线（人读的剧本说明书）
  steps/demo-script.json   # 步骤剧本（三件套结构）
  fixtures/state-000.json  # 每步状态快照
  overlay.tsx              # 视觉引导层（高亮/提示/步进器）
website/...ai-studio 测试或 playwright spec：剧本四项断言
docs/guides/<本文件>.md     # 本方法论，须登记进 docs/README.md 索引
```

## 十、AI Studio 示范剧本（可直接作第一条主线）

前提：演示环境已造好三态项目（ACP-725）。

- **主线「会员积分系统」**：进项目 → 版本历史看 v2 的 diff（含"返回编辑"）→ 回编辑器改两行 → 防抖自动保存 → diff 图标亮起 → 打开红绿 diff → 提交 → 修改历史清空、版本历史 +1、图标回灰
- **分支 A「App 官网改版」**：有未提交草稿 → 打开修改历史列表 → 从某条自动保存**恢复**（演异常/恢复分支）
- **分支 B「空项目示例」**：diff 图标灰态 + 两个历史面板空态（演"为什么是灰的"）
